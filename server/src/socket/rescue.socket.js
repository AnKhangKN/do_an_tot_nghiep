module.exports = (io, socket) => {

    // Tạo sos
    socket.on("sos:create", async () => {
        
    });

    // Nhận sos
    socket.on("sos:accept", async () => {
        
    });

    // Hoàn thành sos
    socket.on("sos:complete", async () => {
        
    });

};